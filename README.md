# 🚗 Traffic Analysis & Visualization Toolkit

Этот инструмент предназначен для **автоматического анализа дорожного трафика** на основе данных, полученных с радиолокационных или аналогичных датчиков. Скрипт обрабатывает временные и пространственные данные, выявляет траектории транспортных средств (ТС), классифицирует их по типу (легковые/грузовые), оценивает скорость и загруженность дороги, а также генерирует наглядные визуализации и отчёты.

---

## 📦 Установка зависимостей

Убедитесь, что у вас установлен Python 3.8+ и выполните:

```bash
pip install h5py numpy matplotlib scipy scikit-image seaborn
```

> **Примечание:** Для работы в headless-режиме (без GUI, например, на сервере) используется `matplotlib.use('Agg')`, поэтому дополнительная настройка не требуется.

---

## 📁 Входные данные

Скрипт ожидает **HDF5-файл** со следующей структурой:

- `/statistics` — трёхмерный массив формы `(T, L, F)`, где:
  - `T` — количество временных шагов,
  - `L` — количество пространственных ячеек (расстояние),
  - `F = 96` — количество частотных каналов.
- `/timestamps` — массив временных меток:
  - либо одномерный массив Unix-времён (`float`),
  - либо двумерный массив формы `(T, 3)` с колонками `[секунды, миллисекунды, ...]` (скрипт использует первые две).

Пример пути к данным: `data/traffic_data.h5`.

---

## 🚀 Быстрый запуск

```python
from traffic_visualization import generate_all_visualizations

result = generate_all_visualizations(
    combined_data_path="path/to/your/data.h5",
    output_dir="output/traffic_analysis"
)

print("Анализ завершён!")
print(f"Обнаружено ТС: {result['tracks_count']}")
print(f"Время обработки: {result['processing_time']:.2f} сек")
```

Результаты сохраняются в указанную директорию.

---

## 📤 Выходные файлы

После выполнения в папке `output_dir` появятся:

- `tracks.json` — полные данные по всем обнаруженным трекам (время, позиция, тип ТС, скорость).
- `report.txt` — краткий текстовый отчёт с ключевыми метриками.
- `infographic.png` — сводная инфографика (типы ТС, распределение скоростей и т.д.).
- Heatmap трафика (возвращается как base64-строка в результате, но может быть сохранён при необходимости).

---

## 📊 Основные возможности

- 🔍 **Обнаружение треков** с использованием преобразования Хафа.
- 🚙 **Классификация ТС** на легковые и грузовые на основе амплитуды сигнала.
- ⏱️ **Оценка скорости** (в км/ч) по перемещению по дистанции.
- 📉 **Анализ загруженности**: выявление заторов (скорость < 25 км/ч).
- 🕒 **Почасовой анализ** интенсивности движения.
- 🎨 **Автоматическая визуализация**: heatmap траекторий и инфографика.

---

## ⚙️ Настройка параметров

Основные константы можно изменить в начале файла:

| Параметр | Значение по умолчанию | Описание |
|--------|----------------------|--------|
| `TIME_STEP` | `0.62` | Временной шаг между измерениями (сек) |
| `DISTANCE_METERS` | `4000` | Общая длина наблюдаемого участка (м) |
| `MIN_TRACK_LENGTH` | `8` | Минимальная длина трека (в точках) |
| `MIN_SPEED_KMH` / `MAX_SPEED_KMH` | `5` / `120` | Диапазон допустимых скоростей |
| `CONGESTION_SPEED_THRESHOLD` | `25` | Порог скорости для определения затора (км/ч) |

Частотные веса (`FREQ_WEIGHTS`) позволяют акцентировать внимание на каналах, характерных для разных типов ТС.

---
